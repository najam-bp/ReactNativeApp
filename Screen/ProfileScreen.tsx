import {View, Text} from 'react-native';
import React from 'react';

const ProfileScreen = () => {
  return (
    <View>
      <Text style={{fontSize: 40, color: 'black'}}>UserScreens</Text>
    </View>
  );
};

export default ProfileScreen;
